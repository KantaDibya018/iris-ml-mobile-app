# Iris ML Mobile App

This project trains multiple ML models on the Iris dataset and uses a Kivy mobile app to predict flower species.

## Features
- Random Forest, Decision Tree, KNN, ANN
- Mobile app with prediction UI
- Image display for predicted flower

## How to Run
1. Train models:
   python brain.py

2. Run app:
   python mobile_app.py
